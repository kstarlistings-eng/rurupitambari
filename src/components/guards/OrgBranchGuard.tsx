// import { useIsBranch } from "@/hooks/useIsBranch";
// import { routerBranch } from "@/routes/BranchRoute";
// import { routerOrg } from "@/routes/OrgRoute";

// import { createBrowserRouter } from "react-router";

// type Props = {
//   children: (router: ReturnType<typeof createBrowserRouter>) => React.ReactNode;
// };

// function OrgBranchGuard({ children }: Props) {
//   const isBranch = useIsBranch();
//   return isBranch ? children?.(routerBranch) : children?.(routerOrg);
// }

// export default OrgBranchGuard;
